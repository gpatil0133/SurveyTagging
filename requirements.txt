# Core runtime
anthropic>=0.83.0
litellm>=1.30

pydantic>=2.5
pydantic-settings>=2.1

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.7

click>=8.1
pyyaml>=6.0
jinja2>=3.1
structlog>=24.1

# Tenant profile fetcher (Parallel.ai)
parallel-web>=0.5.0

pandas>=2.1
pyarrow>=14.0

# SMB client — lets SHARE_ROOT be a UNC path with no CIFS mount
smbprotocol>=1.15

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
