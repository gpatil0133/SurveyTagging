# GENERATED from pyproject.toml [project.dependencies] — edit that, not this.
#
# Kept because deployment installs with `pip install -r requirements.txt`. It
# drifted badly before: numpy, rapidfuzz, httpx, PyJWT and truststore were all
# missing here, so a fresh deploy booted and then failed on first use. Dev/test
# extras live in pyproject's [dev] extra.

pydantic>=2.5
pydantic-settings>=2.1
pyarrow>=14.0
smbprotocol>=1.15
pandas>=2.1
litellm>=1.30
jinja2>=3.1
click>=8.1
structlog>=24.1
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.7
parallel-web>=0.5.0
numpy>=1.26
rapidfuzz>=3.0
httpx>=0.27
PyJWT[crypto]>=2.8.0
truststore>=0.9
